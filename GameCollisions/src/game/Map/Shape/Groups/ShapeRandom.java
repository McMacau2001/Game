package game.Map.Shape.Groups;

import game.Map.Layers.Group;
import game.Map.Shape.Shape;

public class ShapeRandom extends ShapeGroup {

	public ShapeRandom(Shape shape, Group group) {
		super(shape, group);
	}
	
}
